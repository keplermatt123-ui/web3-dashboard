## Packages
framer-motion | Complex animations and scroll triggers
clsx | Utility for conditional class names (standard with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes (standard with shadcn)
lucide-react | Icon set (already in base, confirming usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
  mono: ["'Space Mono'", "monospace"],
}
